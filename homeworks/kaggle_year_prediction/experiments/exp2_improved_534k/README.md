# Эксперимент 2: Улучшенная модель (534K параметров)

## 📊 Характеристики

- **Название**: Improved Model
- **Параметры**: 534,145
- **Архитектура**: 90 → 512 → 384 → 256 → 128 → 64 → 1
- **Val RMSE**: 9.25 лет
- **Время обучения**: ~1.8 минуты
- **Эпохи**: ~200

## 🏗️ Архитектура

```
Input (90)
  ↓
Linear(90, 512) + BatchNorm + ReLU + Dropout(0.3)
  ↓
Linear(512, 384) + BatchNorm + ReLU + Dropout(0.3)
  ↓
Linear(384, 256) + BatchNorm + ReLU + Dropout(0.3)
  ↓ + Residual(512→256)
Linear(256, 128) + BatchNorm + ReLU + Dropout(0.3)
  ↓
Linear(128, 64) + BatchNorm + ReLU + Dropout(0.3)
  ↓ + Residual(256→64)
Linear(64, 1)
  ↓
Output (year)
```

## ⚙️ Гиперпараметры

- **Optimizer**: AdamW
- **Learning Rate**: 0.001
- **Weight Decay**: 1e-5
- **Batch Size**: 128
- **Dropout**: 0.3
- **Scheduler**: ReduceLROnPlateau (factor=0.5, patience=10)
- **Early Stopping**: patience=25

## 📈 Результаты

- **Validation RMSE**: 9.25 years
- **Prediction Range**: 1837 - 2086
- **Mean Prediction**: ~1950

✅ **Хороший результат**: Предсказания в разумном диапазоне, хорошее качество.

## 📁 Файлы

- `train.py` - скрипт обучения
- `model.pth` - сохраненная модель (6.5 MB)
- `submission.csv` - файл для Kaggle

## 🎯 Выводы

Сбалансированная модель с хорошим соотношением качества и скорости обучения. Подходит для быстрых экспериментов с приемлемым качеством.
