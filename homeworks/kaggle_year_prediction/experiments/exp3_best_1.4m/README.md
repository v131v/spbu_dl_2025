# Эксперимент 3: Максимальная модель (1.4M параметров)

## 📊 Характеристики

- **Название**: Best Model
- **Параметры**: 1,404,737
- **Архитектура**: 90 → 1024 → 512 → 384 → 256 → 128 → 64 → 1
- **Val RMSE**: 9.14 лет
- **Время обучения**: ~6.1 минуты
- **Эпохи**: ~500

## 🏗️ Архитектура

```
Input (90)
  ↓
Linear(90, 1024) + BatchNorm + ReLU + Dropout(0.35)
  ↓
Linear(1024, 512) + BatchNorm + ReLU + Dropout(0.35)
  ↓
Linear(512, 384) + BatchNorm + ReLU + Dropout(0.35)
  ↓ + Residual(1024→384)
Linear(384, 256) + BatchNorm + ReLU + Dropout(0.35)
  ↓
Linear(256, 128) + BatchNorm + ReLU + Dropout(0.35)
  ↓ + Residual(384→128)
Linear(128, 64) + BatchNorm + ReLU + Dropout(0.35)
  ↓ + Residual(128→64)
Linear(64, 1)
  ↓
Output (year)
```

## ⚙️ Гиперпараметры

- **Optimizer**: AdamW
- **Learning Rate**: 0.0008
- **Weight Decay**: 1e-5
- **Batch Size**: 128
- **Dropout**: 0.35
- **Scheduler**: ReduceLROnPlateau (factor=0.5, patience=15)
- **Early Stopping**: patience=50

## 📈 Результаты

- **Validation RMSE**: 9.14 years
- **Prediction Range**: 1929 - 4515
- **Mean Prediction**: ~2500

⚠️ **Проблема**: Несмотря на лучший RMSE, есть выбросы за пределы разумного диапазона.

## 📁 Файлы

- `train.py` - скрипт обучения
- `model.pth` - сохраненная модель (17 MB)
- `submission.csv` - файл для Kaggle

## 🎯 Выводы

Мощная модель с лучшим RMSE, но склонна к переобучению и дает некоторые нереалистичные предсказания. Требует дополнительной регуляризации.
