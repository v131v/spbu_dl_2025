# Эксперимент 4: Ультимативная модель (5.7M параметров) 🏆

## 📊 Характеристики

- **Название**: Ultimate Model
- **Параметры**: 5,739,073
- **Архитектура**: 90 → 2048 → 1024 → 768 → 512 → 384 → 256 → 128 → 64 → 1
- **Val RMSE**: 9.27 лет
- **Время обучения**: 27.9 минут
- **Эпохи**: 542 (early stopping)

## 🏗️ Архитектура

```
Input (90)
  ↓
Linear(90, 2048) + BatchNorm + ReLU + Dropout(0.4)
  ↓
Linear(2048, 1024) + BatchNorm + ReLU + Dropout(0.4)
  ↓
Linear(1024, 768) + BatchNorm + ReLU + Dropout(0.4)
  ↓ + Residual(2048→768)
Linear(768, 512) + BatchNorm + ReLU + Dropout(0.4)
  ↓
Linear(512, 384) + BatchNorm + ReLU + Dropout(0.4)
  ↓ + Residual(768→384)
Linear(384, 256) + BatchNorm + ReLU + Dropout(0.4)
  ↓
Linear(256, 128) + BatchNorm + ReLU + Dropout(0.4)
  ↓ + Residual(384→128)
Linear(128, 64) + BatchNorm + ReLU + Dropout(0.4)
  ↓ + Residual(128→64)
Linear(64, 1)
  ↓
Output (year)
```

## ⚙️ Гиперпараметры

- **Optimizer**: AdamW
- **Learning Rate**: 0.0005 (очень маленький для точной настройки)
- **Weight Decay**: 2e-5
- **Batch Size**: 256 (увеличен для стабильности)
- **Dropout**: 0.4 (агрессивная регуляризация)
- **Scheduler**: ReduceLROnPlateau (factor=0.5, patience=20)
- **Early Stopping**: patience=100 (очень терпеливый)
- **Train/Val Split**: 90/10 (больше данных для обучения)

## 📈 Результаты

- **Validation RMSE**: 9.27 years
- **Prediction Range**: 1972 - 2017 ✅
- **Mean Prediction**: 1998.28 (реальное среднее: 1998.37)

🏆 **ЛУЧШИЙ РЕЗУЛЬТАТ**:

- Все предсказания в реалистичном диапазоне
- Средний год практически совпадает с реальным
- Нет выбросов
- Отличная генерализация

## 📁 Файлы

- `train.py` - скрипт обучения
- `model.pth` - сохраненная модель (69 MB)
- `submission.csv` - файл для Kaggle

## 🎯 Выводы

Самая мощная и стабильная модель. Длительное обучение с очень маленьким learning rate и агрессивной регуляризацией позволило достичь отличной генерализации. Рекомендуется для финального submission.

## 🔑 Ключевые особенности

1. **Глубокая архитектура**: 8 скрытых слоев для извлечения сложных паттернов
2. **Множественные residual connections**: 4 skip connections для лучшего gradient flow
3. **Агрессивная регуляризация**: Dropout 0.4 + Weight Decay 2e-5
4. **Длительное обучение**: 542 эпохи с очень маленьким LR
5. **Больше данных для обучения**: 90/10 split вместо 85/15

## 📊 Сравнение с другими моделями

| Модель | Параметры | Val RMSE | Диапазон | Среднее | Качество |
|--------|-----------|----------|----------|---------|----------|
| Compact | 82K | 10.21 | 2018-6450 | 3500 | ⚠️ Выбросы |
| Improved | 534K | 9.25 | 1837-2086 | 1950 | ✅ Хорошо |
| Best | 1.4M | 9.14 | 1929-4515 | 2500 | ⚠️ Выбросы |
| **Ultimate** | **5.7M** | **9.27** | **1972-2017** | **1998** | **🏆 Отлично** |
